@{
    ViewData["Title"] = "Поиск";
}

@if (Model.Count == 0)
{
    <div class="row text-center">
        <div class="col">
            <h3>По искомому запросу ничего не найдено</h3>
        </div>
    </div>
}
else
{
    @model List<ProductViewModel>
    @await Html.PartialAsync("Products")
}